# Use an official Python image
FROM python:3.10-slim

# Set the working directory to match your module structure
WORKDIR /app/packages/backend/api

# Copy requirements file and install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the backend code
# Note: We're copying from the current context to preserve module structure
COPY . .

# Expose the port the app runs on
EXPOSE 8000

# Command to run the application
# Note: We specify the host and port to make it accessible outside container
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]